#include "pch.h"
#include <iostream>

int main()
{
    // Declare int counts for strings with exactly 2 or 3 of any letter

	// Build an array/list/vector/map of ints, one for each letter in the alphabet

	// LOOP for every line in the input file
		// LOOP for every character in the string
			// Process each character one-by-one and increase the associated int counter
			// When done processing this line, if any letters are 2 or 3, increase counts accordingly
		// END LOOP
	// END LOOP
}